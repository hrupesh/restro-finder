import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export default function Restro({restro}) {
    return (
        <View>
            <Text>Name is {restro}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
